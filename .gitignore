node_modules/
*.pre-wizard
# Generated, self-extracting USB installer (rebuild with make-usb-installer.sh).
# Never commit: it's a multi-MB base64 bundle that embeds sibling repos.
capsule-install.sh
*.tar.gz
